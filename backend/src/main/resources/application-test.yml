spring:
  datasource:
    url: jdbc:h2:mem:shopverse_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: false

app:
  jwt:
    secret: TestSecretKeyForUnitTestsOnlyMinimum256BitsLongNotForProduction
    expiration-ms: 3600000
  admin:
    email: testadmin@shopverse.com
    password: TestAdmin@123
